export interface Theme {
  name: string;
  mainColor: string;
  linkColor: string;
  errorColor: string;
  systemBackgroundColor: string;
  textColor: string;
  margin: string;
  padding: string;
  borderRadius: string;
  gap: string;

}
